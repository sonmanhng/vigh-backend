services:
  - type: web
    name: vigh-backend
    runtime: node
    buildCommand: npm install --include=dev && npx prisma generate && npm run build && cd frontend && npm install && npm run build && cd ..
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
